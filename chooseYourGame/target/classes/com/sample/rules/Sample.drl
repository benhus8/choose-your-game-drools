package com.sample.rules

import com.sample.WindowState;
import com.sample.DroolsTest;
 
rule "Init"
	when
	then
		insert(new WindowState(""));
end

rule "Update Window State"
when
    $oknoStan : WindowState(text == null || text == "")
then
    $oknoStan.setText("Płeć osoby : ");
    update($oknoStan);
end

